# Session handoff — 2026-05-26

> Read this if you're starting a new Claude Code session and want to
> pick up where the previous one stopped. Pair this with `STATUS.md`
> for live state. (Supersedes `HANDOFF-2026-05-25.md`.)

## TL;DR — where we are

The V5 bento dashboard was **ported to production and then redesigned into a
"clean" skin (V6)** based on Mohammed's live feedback, **merged to `main`, and
deployed to production**. `antagna-v2.vercel.app/dashboard` now shows it.

The session ended mid-iteration: Mohammed reviewed the live dashboard on a
**wide desktop** and flagged two things to fix next, and is **installing new
MCP servers** to help with the redesign. **Do not start the dashboard rework
until he confirms the MCPs are installed** (he said "استنى" / wait).

## ⏸️ What the next session is waiting on

1. **New MCP servers** — Mohammed is adding some MCPs he thinks will help the
   UI work. They won't appear as tools until **Claude Code restarts**. On the
   next session: confirm which MCPs are live (check the tool list / system
   reminders), then use them for the redesign below.

## 🔥 Hot follow-ups (the actual asks)

1. **Desktop layout is too empty / not organized on wide screens.** From his
   screenshot:
   - The board is capped at `max-w-[1400px]` → big empty side margins on wide
     monitors. Consider widening / fluid max-width / denser columns.
   - The bento leaves **vertical gaps** because card heights differ and empty
     cards (e.g. "توقّع حمولة الفريق" → "لا توظيفات نشطة", "صحة المشاريع" with
     blank space) stretch tall. Needs: smarter size distribution, better
     width fill, and **compact empty states** so empty cards don't bloat.
   - Files: `apps/web/src/app/dashboard/page.tsx` (grid container + max-width
     lives in the `Shell` / page), `dashboard-grid.tsx` (the 12-col grid),
     the card components in `dashboard/cards/cards.tsx` (empty-state markup).
2. **Restructure the right side nav (dock).** Mohammed wants it re-organized.
   Get his spec first (sections / order / icons+labels). The dock lives in
   `apps/web/src/components/Shell.tsx` (AppShell).
3. Then: **spread the clean skin** to `/projects`, `/inbox`, `/equipment`,
   `/team` so the dashboard isn't an island.
4. Wire the remaining catalog cards (hot_leads, lead_temp, ai_cost,
   oauth_health, activity, open_tasks…) to real Supabase queries.
5. Replace the **heuristic** `project_health` / `at_risk` scoring with a real
   LLM/AI scorer (currently deterministic, shown under the AI badge).
6. PWA attendance check-in (D-031) — still in backlog behind this.

## ✅ What shipped this session (D-033 → D-036)

- **Shared module** `apps/web/src/app/dashboard/cards/`:
  - `utils.tsx` — pure helpers (`cardSpanClass`, `toAr`, `Cell`, size/AI types). **Server-safe** (kept out of the client shell on purpose).
  - `shell.tsx` (`'use client'`) — `Card`, `AIBadge`, and the **`CardSkin` context** with two skins: `mono` (V5 default) and `clean` (V6).
  - `cards.tsx` — 28 cards + `CardGlance`; wired cards take optional `data` with sample fallback (so preview labs still render).
  - `catalog.ts` — `CARD_CATALOG` (now with `titleAr` clean Arabic titles), `DEFAULT_LAYOUT`, `resolveLayout`, `DASH_LAYOUT_COOKIE`.
- **Production `/dashboard`** (`page.tsx` + `dashboard-grid.tsx`): real Supabase
  queries → 10 live cards, BriefingCard hero, clean skin, **Framer Motion**
  (spring entrance, hover lift, layout reflow), per-card **quick actions** on
  hover, and a full **customize** system (size cycle, drag/▲▼ reorder,
  add/hide) persisted in the `dash_layout` cookie via server action.
  - Native drag handlers live on an **inner `<div>`** because Framer's
    `motion.div` reserves `onDragStart/End`.
- **Fonts:** clean cards use **Vazirmatn** (the title font Mohammed liked) for
  body too; numbers/timecodes stay Geist Mono. Vazirmatn now loads 400/500/600/700.
- **Contrast:** `.card-clean` scoped CSS in `globals.css` boosts muted greys.
- Old `dashboard-customize.*` (show/hide-only) removed.
- **Previews:** `/preview/lab/v5` (mono reference, re-exports the shared module),
  `/preview/lab/v6` (motion + clean skin), `/preview/lab/v6/stitch` (Google
  Stitch concept image at `public/v6-stitch-concept.png`).

## 🎨 Design rules now (D-034 holds; D-036 updates titles)

- ✅ Single accent orange `#FF6B1A`; backgrounds `#0F0F12` / `#17171C`;
  hairline borders; **no** chromatic/mesh gradients, glow, or frosted glass.
- 🔁 **Clean skin supersedes the `// snake_case` mono titles** (D-036): card
  titles are clean human Arabic in Vazirmatn. The mono look lives only in
  `/preview/lab/v5`.
- ✅ Motion = physics (entrance/hover/reflow), not decoration. Drag stays
  behind the customize ("ترتيب") mode so it never hijacks scrolling.
- Full taste notes: `memory/feedback_design_taste.md`.

## ☁️ Important production / infra state

- **Live:** <https://antagna-v2.vercel.app> (the dashboard is here). Prod deploy
  is NOT behind Vercel protection (team can access).
- **Preview deploys** need env on the **Preview** scope — this project has no
  git connection, so set vars with the empty-branch trick
  `vercel env add NAME preview "" --value … --yes` (needs Vercel CLI ≥ 54.4).
  16 runtime vars already set for preview. See `memory/project_vercel_preview_env.md`.
- Stable preview aliases used for review: `antagna-v5.vercel.app`,
  `antagna-v6.vercel.app`.
- Functions pinned `hnd1`; Supabase `nicijexpmpekzuzevarf` (ap-northeast-1);
  real pooler `DATABASE_URL` in repo-root `.env.local`.
- Branch `feat/dashboard-v5-port` is merged into `main` (can be deleted).

## Recent commits

```
c0cb8e8 Merge V5→prod dashboard + V6 clean skin & motion (D-033, D-036)
eb9152d docs: D-036 dashboard shipped (V6 clean skin) + STATUS
60bcae1 dashboard(prod): apply V6 clean skin + motion + quick actions
d4fd845 v6: unify clean cards on Vazirmatn (title font) for body too
bd01441 v6: clean card skin (Arabic titles, contrast, quick actions) + fix drag/scroll
2710a39 preview/lab/v6: antigravity motion dashboard + Stitch concept
469d3fc dashboard: port V5 bento to production (D-033)
```

## Where to look first

| What | Where |
|------|-------|
| Live state | `STATUS.md` |
| Why (latest) | `decisions-log.md` → **D-036** |
| Visual taste rules | `memory/feedback_design_taste.md` |
| Shared cards module | `apps/web/src/app/dashboard/cards/` |
| Production dashboard | `apps/web/src/app/dashboard/page.tsx` + `dashboard-grid.tsx` |
| Clean-skin CSS / fonts | `apps/web/src/app/globals.css` (`.card-clean`) · `layout.tsx` |
| Side nav (to restructure) | `apps/web/src/components/Shell.tsx` |
| V6 motion + Stitch concept | `apps/web/src/app/preview/lab/v6/*` |
| Preview env quirk | `memory/project_vercel_preview_env.md` |
