/**
 * The V5 card library now lives in the production module at
 * `apps/web/src/app/dashboard/cards/`. This file re-exports it so the
 * preview labs and the production dashboard share one source of truth.
 */
export * from '@/app/dashboard/cards';
