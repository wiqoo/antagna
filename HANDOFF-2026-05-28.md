# Session handoff — 2026-05-28

> Read this if you're starting a new Claude Code session and want to pick up
> where the previous one stopped. Pair this with `STATUS.md` for live state
> + `PHASE-2-PLAN.md` for the 12-week roadmap. Supersedes `HANDOFF-2026-05-26.md`.

## TL;DR — where we are

A **massive consolidation session**. The Cowork audit proposal landed, the
permissions architecture doc landed, Mohammed gave scoped permission to read
the live volt-os DB, and we sealed 4 strategic decisions (D-037 → D-040).

- 🐛 **Cowork-audit bug sweep closed** — 15 items reviewed, 11 fixed live, 4
  marked NOT-REPRODUCIBLE (proposal was 3 commits stale).
- 📐 **PHASE-2-PLAN.md** locked: Sprint 0 (Permissions architecture, 4w) +
  Sprints 1-4 (8w) = **12 weeks total**. 4 decisions logged.
- 📦 **162 equipment imported** from volt-os live DB (Mohammed's explicit OK).
  136 with photos, 148 with AI-meta from volt-os's earlier extraction.
- 📧 **/inbox/[threadId]** built — per-thread page with AI summary, AI
  next-actions (with `retrieveMemory()` pulling client-scoped chunks), full
  conversation render, follow-up state. **First codebase use of Anthropic
  prompt caching** (`cache_control: ephemeral` on system blocks).
- 🪪 **/clients/new** rebuilt — code auto-generated, type clarified, new
  "للحساب" (Volt / Abu Luka) selector, industry as 6-preset dropdown.
- 🛠️ **Equipment polish** — mobile dock "more" popover anchored to viewport,
  AIHints buttons now route to `?status=` filters, specs render groups +
  hides empty + flattens `ai_meta` (no more `[object Object]`).
- 🚦 **View As fixed** — was gating on the phantom `system_manager` role; now
  matches Shell's `ADMIN_ROLES` (system_admin + general_manager). Mohammed
  felt it "did nothing" — that's why.
- 🧭 **/attendance + /whatsapp added to nav** — both pages existed for days,
  weren't reachable from the side dock.
- 🛡️ **Permissions architecture spec** (`01PERMISSIONSforClaudeCode.md`)
  digested. 16-position × field-masking matrix is the Sprint 0 deliverable.

## 🔑 Mohammed's chosen execution mode: **Option B (Parallel)**

End of session he picked parallel execution:

- **Sprint 0 (Permissions architecture)** runs in the background through
  Weeks 1-4.
- **Batches 2 + 3 + 4** (visible UI value) build alongside Sprint 0 — Mohammed
  + admins use the system while the rest of the team waits for permissions
  to land before rollout.

Trade-off accepted: pages built in Batches 2/3/4 will need light refactor in
Phase D to read from `v_*_safe` views instead of base tables.

## 📋 The 7 batches (from the plan I delivered)

| # | Batch | Status | Effort |
|---|---|---|---|
| 1 | Sprint 0 — Permissions (Phases A→H) | **runs in parallel** | 4 weeks |
| 2 | `/admin/system` (API tokens, ai_usage costs, cron, subscriptions, system_settings) | start | 2 days |
| 3 | Project flows (file upload + AI analyze, equipment reservation, repairs) | start | 3 days |
| 4 | UI polish (equipment view-toggle/sort/category-filter, mobile dashboard, inbox filters, dupe wiring) | start | 2-3 days |
| 5 | Hidden entities (12+ tables with no UI today) | follow | 1-2 weeks |
| 6 | AI/Brain expansion (prompt caching everywhere, memory wiring deeper) | follow | 1 week |
| 7 | Verification (CI, audit specs, mobile/a11y, View As validation) | last | 2 days |

Total: ~7-8 weeks calendar time (parallel batches).

## 🛡️ Sprint 0 decisions sealed (D-037 → D-040 in decisions-log.md)

- **D-037** — Adopt the 16-position × field-masking architecture wholesale
  from `01PERMISSIONSforClaudeCode.md`.
- **D-038** — Rename: existing `capabilities` table (skills catalog: shooter,
  editor, drone_pilot…) → `skills`. Reclaim `capabilities` for the new
  fine-grained access codes. `user_capabilities` → `user_skills`.
- **D-039** — Enforcement: **app-layer `can()` + safe views**, NOT user-token
  RLS. Keep service-role connection. RLS stays enabled as belt-and-suspenders
  but isn't the primary gate.
- **D-040** — Auth model: **invite-only** (supersedes the earlier verbal
  "open self-signup" pick that conflicted with strict positions).

## 🚀 Where to start the next session

**Pick one of these and execute. Each is self-contained.**

### Option 1 — Sprint 0 Phase A (Permissions vocabulary reconciliation)
Files: new migration 048+, `packages/db/src/schema/people.ts`,
`apps/web/src/lib/authz.ts`, then a sweep of ~15 files using
`capabilities` to switch to `skills`. Adds 16 positions seeded into
`positions` table. **Touches schema; deploy carefully.**

### Option 2 — Batch 2: `/admin/system` page
Files: `apps/web/src/app/admin/system/page.tsx` + per-section components.
Sections:
1. **API tokens** (oauth_tokens table, 0 rows today — read env for Anthropic
   / OpenAI / Vercel / Supabase / Resend / Google / WhatsApp, show health
   check + expiry where available).
2. **AI costs** — visualize `ai_usage` (771 rows): per-feature chart, per-
   model, total $ this month, cache-savings now that prompt caching is on.
3. **Cron heartbeat** — read `cron_heartbeat`, show last-run per scheduler
   + the piggyback chain (alert-scanner → media-scanner → alert-notifier;
   insights-scanner → memory-indexer → learning-aggregator → deadline-notifier;
   daily-brief → smart-suggestions → daily-digest).
4. **Subscriptions** — calendar-style table of paid services (Vercel Pro,
   Supabase Pro, Trigger.dev Pro, Anthropic, OpenAI, Resend, WPPConnect VPS,
   Gmail API quota) with renewal dates + monthly cost. **No DB table yet —
   model in `system_settings.subscriptions` jsonb.**
5. **System settings** — generic key/value editor for `system_settings`.

### Option 3 — Batch 3: Project flows
1. **File upload on /projects/new and /briefs/new** — `<input type="file"
   multiple accept="application/pdf,image/*,text/*">` → upload to a new
   `project-attachments` Storage bucket → Claude (vision for images, text
   for PDFs via `pdf-parse`) extracts client/budget/dates/deliverables →
   pre-fills the wizard. Stash refs in `attachments` (entity_type='project'
   or 'brief', `ai_extracted` jsonb).
2. **"احجز معدّة" button** on `/equipment/[id]` — modal/form (start/end
   dates, project selector, notes) → INSERT `equipment_reservations` +
   call `fn_check_reservation_lead_time` for warnings + writeActivity.
3. **Repair form** on `/equipment/[id]` — opens `equipment_repairs` row
   with ETA, fault description, fixer.

### Option 4 — Batch 4: UI polish (the most-visible)
1. Equipment list: view toggle (cards-with-photos / table / compact) + sort
   + manual category chip filter + search bar.
2. Equipment detail: fix QR card layout (currently overlaps the explanation
   text — Mohammed's screenshot).
3. Mobile dashboard: bento needs a real mobile layout (top 3 cards visible,
   rest collapsed into "اعرض الكل").
4. Inbox list: add search + filter (status/client/project/assigned) + sort.
5. **Wire dupe detection** — `lib/dupes.ts` + migration 047 are deployed
   already. Just need to call `findClientDuplicates()` / `findProjectDuplicates()`
   / `findContactDuplicates()` from the createX actions, return a structured
   result, and render an alert in the form with "use existing" / "force create".

## ⏸️ What's waiting on Mohammed (manual items)

- **Geofence coordinates** for office + recurring shoot locations (Phase F
  attendance UX). Just need lat/lng + radius per location.
- **Sentry token with `event:read` scope** — current release-only token can't
  read issues. Generate a wider one in Sentry settings.
- **DNS flip** `antagna.me` → Vercel when ready to retire the `.vercel.app`
  URL. (Vercel project is `antagna-v2`.)

## ✅ What shipped this session (the commit ledger)

- `9a5d949` view-as admin gate fix + /attendance + WhatsApp in nav
- `577d5fb` inbox per-thread detail + AI summary/next-actions + prompt caching
- `1652d8d` /clients/new — code auto-gen, sub-brand selector, industry dropdown
- `90dd7e2` equipment — mobile dock popover, AIHint filter wiring, specs render
- `20e7112` bug-sweep follow-through: #6 (6 KPI handlers) / #11 / #14 / #15
- `c02a910` checklist refresh — bug sweep + PHASE-2-PLAN + decisions
- `dd286f1` data: import 162 equipment from volt-os live (D-040 + scope OK)
- `8a9d4f0` plan + decisions: Sprint 0 (Permissions architecture) inserted
- `55fc51d` PHASE-2-PLAN — 8-week roadmap baseline
- `702817e` Cowork-audit P0 + LOW bug sweep
- `e4ab643` i18n: t() wired into 5 page headers
- `c227582` tests: 3 more E2E specs + CI jobs (unit + e2e)
- `13bd40f` notify: 3 worker schedulers + @mentions
- `cc69f7f` C1 kits CRUD

Live URL: <https://antagna-v2.vercel.app>. Latest deploy: `9a5d949` (READY).

## 🔍 Audit snapshot (built-in-DB-without-UI)

The "compare what's built vs what's exposed" inventory I delivered to
Mohammed (last user message):

**Tables with no UI today** — `oauth_tokens` (0 rows), `system_settings` (0),
`equipment_reservations` (0), `equipment_repairs` (0), `internal_approvals`
(0), `attachments` (0), `notification_subscriptions` (0), `meeting_notes` (3),
`squads` / `squad_members` (0), `project_recurrence_rules` (0), `project_pins`
(0), `compatibility_rules` (0), `kit_suggestions` (0), `tags` (6, no manager),
`custom_field_definitions` / `custom_field_values` (extensibility — no UI),
`employees` (0, separate from profiles), `departments` (5, no admin),
`external_links`, `stage_task_templates`.

**DB functions with no UI entry point** — `fn_check_project_auto_deliver`,
`fn_check_reservation_lead_time`, `fn_spawn_stage_tasks` (runs on stage
transition but no template editor), `fn_decay_lead_temperature` (cron only),
`fn_explode_squad_assignment` (squads not UI'd).

Each of these is Batch 5 work.

## 🧠 Brain + AI cost status

- `ai_memory_chunks`: **456 chunks** (good baseline).
- Memory indexer ran today (2026-05-28 18:06 UTC).
- `ai_usage`: **771 rows** (no UI to visualize → Batch 2 fixes this).
- Prompt caching: **first wiring landed today** in `/inbox/actions.ts`
  (`summarizeThreadAction`, `generateNextActionsAction`). Cache hit returns
  10% input pricing on the cached block. Other places that should get the
  same treatment in Batch 6: `insights-scanner`, `daily-brief`,
  `parseBriefRich`, `email-intel/generate`, `email-intel/extract`,
  `whatsapp-bot` system prompt.
- `retrieveMemory()` used in: `inbox/generateNextActionsAction` (client scope).
  Should also be wired into: `insights-scanner` (per-project), `daily-brief`
  (per-profile), `parseBriefRich` (client-scoped), `whatsapp-bot`
  (contact-scoped). Batch 6.

## 📁 Recently created or rewritten files (for context)

- `PHASE-2-PLAN.md` — the 12-week roadmap (Sprint 0 + 4 sprints).
- `apps/web/src/lib/dupes.ts` — duplicate finders (client + project + contact)
  using pg_trgm. **Not wired into createX actions yet** — Batch 4 task.
- `supabase/migrations/00000000000045_freelancer_availability.sql`
- `supabase/migrations/00000000000046_whatsapp_media_transcription.sql`
- `supabase/migrations/00000000000047_dupe_detection.sql`
- `apps/web/src/app/inbox/[threadId]/page.tsx` + `inbox/actions.ts`
- `apps/web/src/app/equipment/scan/` + `equipment/kits/`
- `apps/worker/src/trigger/whatsapp-media-scanner.ts`
- `apps/worker/src/trigger/deadline-notifier.ts`
- `apps/worker/src/trigger/alert-notifier.ts`
- `apps/worker/src/trigger/daily-digest.ts`
- `apps/web/src/lib/attendance-queue.ts`
- `apps/web/src/app/api/internal/notify/route.ts`
- `packages/ui/src/components/StatBox.tsx` (consolidation)

## 🎯 First commit of the next session (recommendation)

**If picking Option 2 (Batch 2 — `/admin/system`):**
Start with `apps/web/src/app/admin/system/page.tsx` as a server component
that renders 5 cards (API tokens, AI costs, cron, subscriptions, settings).
Each card reads from its respective source (env probe for tokens, ai_usage
group-by for costs, cron_heartbeat join, hard-coded subscriptions list to
start, system_settings jsonb editor). Add `/admin/system` to the admin landing
sidebar in `apps/web/src/app/admin/page.tsx`.

**If picking Option 1 (Sprint 0 Phase A):**
First commit = `supabase/migrations/00000000000048_rename_capabilities.sql`
that does `ALTER TABLE capabilities RENAME TO skills; ALTER TABLE
user_capabilities RENAME TO user_skills;` + FK + index renames. Second
commit = sweep of ~15 source files importing `capabilities` from
`@antagna/db` → switch to `skills`. Third commit = migration 049
extending `permissions` with the new fine-grained codes + rename
`role_default_permissions` → `position_default_permissions` + seed 16
positions from `01PERMISSIONSforClaudeCode.md` Part 1.

— end of handoff
